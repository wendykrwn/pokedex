<!DOCTYPE html>
<html>

<head>
  <title>
    <%= title %>
  </title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
  <h1>
    <%= pokemon.name %>
  </h1>
  <div>
    <img src=" <%= pokemon.sprites.front_default %>" alt="image">
  </div>
  <table>
    <tr>
      <td>nom</td>
      <td><%= pokemon.name %></td>
    </tr>
    <tr>
      <td>Taille</td>
      <td><%= pokemon.height %></td>
    </tr>
    <tr>
      <td>Abilities</td>
      <td>
        <ul>
          <% pokemon.abilities.forEach(({ability},i)=> { %>
            <li>
              <%= ability.name %>
            </li>
            <% }); %>
        </ul>
      </td>
    </tr>

    <tr>
      <td>Base experience</td>
      <td>
        <%= pokemon.base_experience %>
      </td>
    </tr>
  </table>

  <div>
    <h2>Ajouter au Favoris</h2>
    <form class="form-input" action="/favoris" method="post">
      <input type="text" name="name" value="<%= pokemon.name %>" readonly hidden>
      <input type="number" name="number" value="<%= pokemon.id %>" readonly hidden>
      <div>
        <label for="isOwned">Possédé : </label>
        <input id="isOwned" type="checkbox" name="isOwned">
      </div>
      <div>
        <textarea name="comment" placeholder="Un petit commentaire ? "></textarea>
      </div>
      <div>
        <button type="submit">Ajouter au favoris</button>
      </div>
    </form>
  </div>



</body>

</html>